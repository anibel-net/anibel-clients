# Update package checks

Parent guidance applies. This console tool uses the real Velopack SDK with a test installation location. Supply the local feed, current version, expected version and optional channel. A delta test must fail if it falls back to a full download. Keep all staged files in a unique temporary directory. Never apply an update to the developer installation. This tool does not verify a real installed-app restart.
