<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Anibel.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Anibel.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="12" MaxWidth="560" HorizontalAlignment="Left">
            <TextBlock Text="Налады" Style="{StaticResource TitleTextBlockStyle}" />

            <ComboBox x:Name="BackgroundCombo" Width="220" Header="Фон акна" SelectionChanged="OnBackgroundChanged">
                <ComboBoxItem Content="Суцэльны" />
                <ComboBoxItem Content="Mica" />
                <ComboBoxItem Content="Acrylic" />
            </ComboBox>
            <TextBlock Text="Змяняецца адразу. Калі эфект недаступны, выкарыстоўваецца суцэльны фон."
                       TextWrapping="Wrap" Opacity="0.6" FontSize="12" />

            <ComboBox x:Name="LanguageCombo" Width="220" Header="Мова назваў і апісанняў" SelectionChanged="OnLanguageChanged">
                <ComboBoxItem Content="Беларуская" />
                <ComboBoxItem Content="Русский" />
            </ComboBox>

            <TextBlock Text="Адрасы API (для распрацоўкі)" Opacity="0.6" />
            <TextBox x:Name="ApiUrlBox" Header="GraphQL API" PlaceholderText="https://anibel.net/graphql" />
            <TextBox x:Name="VideoApiUrlBox" Header="Video API" PlaceholderText="https://api.anibel.stream" />
            <Button Content="Захаваць адрасы" Click="OnSaveUrlsClick" />
            <TextBlock Text="Новыя адрасы ўступяць у сілу пасля перазапуску" Opacity="0.5" FontSize="12" />

            <Button Content="Ачысціць кэш відэа" Click="OnClearCacheClick" />
            <Button Content="Ачысціць кэш каталога" Click="OnClearApiCacheClick" />
            <TextBlock Text="Спампоўкі (афлайн-бібліятэка) не выдаляюцца з кэшам — кіруйце імі на старонцы «Спампаванае»."
                       TextWrapping="Wrap" Opacity="0.55" FontSize="12" />

            <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Абнаўленні" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="{x:Bind Updates.VersionLabel}" />
                    <TextBlock Text="{x:Bind Updates.ChannelLabel}" Opacity="0.7" />
                    <TextBlock Text="{x:Bind Updates.StatusLabel, Mode=OneWay}" TextWrapping="Wrap" />
                    <ProgressBar Value="{x:Bind Updates.Progress, Mode=OneWay}" Maximum="100"
                                 Visibility="{x:Bind Updates.IsDownloading, Mode=OneWay}" />
                    <Button Content="Праверыць абнаўленні" IsEnabled="{x:Bind Updates.CanCheck, Mode=OneWay}" Click="OnCheckUpdatesClick" />
                    <Button Content="Перазапусціць і абнавіць" Visibility="{x:Bind Updates.IsReady, Mode=OneWay}" Click="OnRestartUpdateClick" />
                    <HyperlinkButton Content="Што новага" Click="OnReleasesClick" />
                </StackPanel>
            </Border>
            <TextBlock x:Name="Status" Opacity="0.7" Visibility="Collapsed" />
        </StackPanel>
    </ScrollViewer>
</Page>
